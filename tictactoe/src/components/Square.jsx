import React from 'react'

export const Square= ({value}) => { //pass in a destructured value
 
  return (
        
        <button type='button'>{ value }</button>//passed an attribute to this button
    
  )
}
